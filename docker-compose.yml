services :
  redis:
    image: redis:7-alpine
    container_name: live-leaderboard_redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - live-leaderboard_redis:/data

volumes:
  redis_learning_data: